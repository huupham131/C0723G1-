create database demo;

use demo;

create table Student(
 id int,
 name varchar(200),
 age int,
 country varchar(50)
);
create table class(
id int,
name varchar(200)
);
create table Teacher(
id int,
name varchar(200),
 age int,
 country varchar(50)
);
